{
  "name": "lukerobotarm.com",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && powershell -NoProfile -ExecutionPolicy Bypass -File ./deploy-iis.ps1",
    "luke-chat-check": "node server/luke-chat.mjs --self-check",
    "luke-chat": "node server/luke-chat.mjs --listen",
    "luke-ping": "curl.exe -s http://localhost:5173/api/luke-session"
  },
  "devDependencies": {
    "typescript": "~5.8.3",
    "vite": "^7.0.4"
  },
  "dependencies": {
    "js-aruco": "^0.1.0"
  }
}
