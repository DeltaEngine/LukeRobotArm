export default function Connect() {
  const container = document.createElement('div');
  container.innerHTML = `<h2>Connect</h2><p>This is the connect page. Add connection instructions here.</p>`;
  return container;
}
